import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
  cardContainer: {
    marginHorizontal: 30,
    marginVertical: 10,
    padding: 10,
    minWidth: 90,
    maxWidth: 400,
    width: '85%',
    height: 50,
    minHeight: 35,
    justifyContent: 'center',
    alignItems: 'center',
  },
})
